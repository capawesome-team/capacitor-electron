import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.capawesome.example',
  appName: 'Capacitor Electron Example',
  webDir: 'dist',
};

export default config;
